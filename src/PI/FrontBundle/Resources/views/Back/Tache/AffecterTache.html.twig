{% extends "@PIFront/Back/indexBack.html.twig" %}

{% block body %}

    {{ form_start(f) }}

    <div class="form-group">
    <div class="row">
    <div class="col-lg-12">
    <section class="panel">
        <header class="panel-heading">
            <div>
                <class>
                    <h1 class="first-city">Affecter une Tâche</h1>
                </class>
            </div>

        </header>

        <div class="form-group">
            <form class="form-horizontal">


                <br><br>
                <div class="form-group">
                    <label class="col-sm-2 control-label">{{ form_label(f.user) }} </label>
                    <div class="col-sm-10">

                        {{ form_widget(f.user) }}
                    </div>

                    <div><br><br><br>
                        &nbsp;
                        <input a href="{{ path('pi_back_liste_taches') }}" class="btn btn-round btn-primary " value="affecter" type="submit">
                        <a href="{{ path('pi_back_liste_taches_non_affecte') }}" class="btn btn-round btn-danger">Annuler</a>
                    </div>






                    {{ form_end(f) }}

            </form>
        </div>
    </section>
{% endblock %}