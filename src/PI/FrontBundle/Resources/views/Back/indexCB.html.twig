{% extends 'PIFrontBundle:Back:indexBack.html.twig' %}

{% block body %}
    <h1 class="first-city">Liste des congés</h1>
<div class="form-group">
    <div class="row">
        <div class="col-lg-12">
            <section class="panel">
                <div class="panel-body">

    <table class="table table-stripped ">
        <thead>
        <tr>
            <th>Nom</th>
            <th>Date début</th>
            <th>Type du congé</th>
            <th>Etat</th>

            <th>  </th>
            <th></th>


        </tr>
        </thead>

        <tbody>
        {% for conge in conges %}
            <tr>
                <td> {{ conge.user}}</td>
                <td>{% if conge.datedebut %}{{ conge.datedebut|date('Y-m-d') }}{% endif %}</td>

                <td>{{ conge.TypeConge }}</td>
                <td>{{ conge.etat }}</td>

                <td>

<div>
                            <a  class="btn btn-round btn-primary btn-lg" href="{{ path('conge_showB', { 'id': conge.id }) }}">  <i class="fa fa-eye"></i>  Plus de details

                    </a>

</div>



                </td>
                <td>

                    <div>
                        <a  class="btn btn-round btn-primary btn-lg btn-danger" href="{{ path('conge_deleteB', { 'id': conge.id }) }}">  <i class="fa fa-trash-o"></i>  Supprimer

                        </a>

                    </div>



                </td>


            </tr>

        {% endfor %}
        </tbody>

    </table>
                    <a  class="btn btn-default btn-lg" href="{{ path('conge_RechercheDate')}}"> chercher congé Par Date </a>
                    <a  class="btn btn-default btn-lg" href="{{ path('conge_RechercheType')}}"> chercher par type congé </a>

                    </div>
                </section>
            </div>
        </div>
    </div>



{% endblock %}
