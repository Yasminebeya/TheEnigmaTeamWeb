{% extends 'PIFrontBundle:Front:index.html.twig' %}

{% block body %}



    {{ form_start(edit_form) }}

    <div class="form-group">
        <div class="row">
            <div class="col-lg-12">
                <section class="panel">
                    <header class="panel-heading">
                        <div>
                            <class>
                                <h1 class="first-city">Modifier les infos du congé</h1>
                            </class>
                        </div>

                    </header>

                    <div class="panel-body">


                        <div class="form-group">
                            <label class="col-sm-2 control-label">{{ form_label(edit_form.datedebut) }} </label>
                            <div class="col-sm-10">{{ form_widget(edit_form.datedebut) }}</div>
                        </div>

                        <br>
                        <div class="form-group">
                            <label class="col-sm-2 control-label">{{ form_label(edit_form.nbrjours) }} </label>
                            <div class="col-sm-10">{{ form_widget(edit_form.nbrjours) }}</div>
                        </div>


                        <br>

                        <br>
                        <div class="form-group">
                            <label class="col-sm-2 control-label">{{ form_label(edit_form.TypeConge) }} </label>
                            <div class="col-sm-10">{{ form_widget(edit_form.TypeConge) }}</div>
                        </div>
                        <br>
                        <div class="form-group">
                            <label class="col-sm-2 control-label">{{ form_label(edit_form.raison) }} </label>
                            <div class="col-sm-10">{{ form_widget(edit_form.raison) }}</div>

                        </div>
                        </br>
                        </br>
                        <input a href="{{ path('conge_index') }}" class="btn btn-round btn-primary " value="modifier" type="submit">


                        {{ form_end(edit_form) }}
                        <ul>

                            <li>
                                <a href="{{ path('conge_index') }}">Liste des congés</a>
                            </li>
                        </ul>
                    </div>
                </section>
            </div>
        </div>
    </div>
    </form>
{% endblock %}
