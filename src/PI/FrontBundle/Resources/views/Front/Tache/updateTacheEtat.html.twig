{% extends "@PIFront/Front/index.html.twig" %}

{% block body %}
    {{ form_start(f) }}

    <div class="form-group">
        <div class="row">
            <div class="col-lg-12">
                <section class="panel">
                    <header class="panel-heading">
                        <div>
                            <class>
                                <h1 class="first-city">Modifier l'état d'une Tâche</h1>
                            </class>
                        </div>

                    </header>

                    <div class="panel-body">




                        <br>
                        <div class="form-group">
                            <label class="col-sm-2 control-label">{{ form_label(f.etat) }} </label>
                            <div class="col-sm-10">
                                <div class="input">{{  form_widget(f.etat) }}</div>


                            </div>
                        </div>



                    </div>
                    &nbsp;&nbsp;&nbsp;    <input a href="{{ path('pi_front_tache') }}" class="btn btn-round btn-warning" value="Modifier" type="submit">
                    <a href="{{ path('pi_front_tache') }}" class="btn btn-round btn-danger">Annuler</a>

                    {{ form_end(f) }}
                </section>
            </div>
        </div>
    </div>
    </form>

{% endblock %}