
{% extends "@PIFront/Front/index.html.twig" %}

{% block body %}


    <div>
        <class>
            <h1 class="first-city">Message(s)</h1>
        </class>
    </div>
    <div class="form-group">
        <div class="row">
            <div class="col-lg-12">
                <section class="panel">
                    {% for convocation in convocation %}
                    <div id="offre">

                        <div class="col-md-4" >
                            <div class="thumbnail"  style="background-color: #F1EFF2">

                                <label>{{ convocation.datedenvoye|date("d/m/Y") }}</label> <br>
                                <label>Objet </label>  {{ convocation.objet}}<br>
                               {{ convocation.text }} <br><br>





                                <div align="center">
                                    <a href="{{ path('pi_front_supprimer_convocation',{'id': convocation.id}) }}" >
                                        <button class="btn btn-round btn-primary" >Supprimer</button></a>
                                </div>



                            </div>
                        </div>
                    </div>
                    {% endfor %}

                </section>

            </div>
        </div>
    </div>
    </form>

{% endblock %}
